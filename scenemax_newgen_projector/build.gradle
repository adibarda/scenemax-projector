apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    runtime files('libs/antlr-4.5.3-complete.jar')
    runtime files('libs/scenemax_parser.jar')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
